<?php

if (! isset($posts) || ! is_array($posts)) {
    return 'Post data missing!';
}

$view->renderWidget([
    'widget' => \Frontend\Widget\ListingArticles::class,
    'method' => 'renderWidget',
    'params' => [
        'post' => $posts
    ],
]);